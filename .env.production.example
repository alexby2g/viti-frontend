# En Vercel usamos proxy same-origin para que Sanctum mantenga cookies HttpOnly + CSRF.
VITE_API_URL=/api/v1

# Dominio público del bucket R2 usado únicamente para fotos y logotipos.
VITE_MEDIA_URL=https://TU_DOMINIO_PUBLICO_R2
